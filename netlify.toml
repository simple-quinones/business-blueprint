[build]
  publish = "public"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "18"

[functions]
  node_bundler = "esbuild"

[functions."blueprint"]
  timeout = 26

[[redirects]]
  from = "/api/blueprint"
  to = "/.netlify/functions/blueprint"
  status = 200
